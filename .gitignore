# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright E2E output (see docs/TESTING.md)
/playwright-report/
/test-results/
/blob-report/

# Playwright E2E test-account credentials (never commit these — see
# docs/TESTING.md). Copy e2e/.env.example to this file at the repo root.
.env.e2e

# Environment variables (copy .env.example / server/.env.example instead)
.env
server/.env

# Local dev-preview tooling config (not part of the app)
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
